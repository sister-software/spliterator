[package]
name = "spliterator-wasm"
version = "0.1.0"
edition = "2021"
description = "WASM SIMD delimiter scanner for spliterator"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"
lto = true
strip = "symbols"
codegen-units = 1
panic = "abort"
